# Expectations for expressjs/express benchmark.
# Express uses mocha with files in test/ — moderate backend JS repo.
# Baselines from 2026-03-06 run.
min_test_files: 50
min_code_units: 1
require_posture: true
analyze_must_succeed: true
portfolio_must_succeed: true
migration_must_succeed: true
