# Expectations for jestjs/jest benchmark.
# Large monorepo, tests itself with Jest. 1050+ test files.
# Baselines from 2026-03-06 run.
min_test_files: 500
min_code_units: 100
require_posture: true
analyze_must_succeed: true
portfolio_must_succeed: true
migration_must_succeed: true
