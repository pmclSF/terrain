# Expectations for pallets/flask benchmark.
# Python repo with pytest — exercises cross-language analysis.
# Baselines from 2026-03-06 run.
min_test_files: 20
min_code_units: 30
require_posture: true
analyze_must_succeed: true
portfolio_must_succeed: true
migration_must_succeed: true
