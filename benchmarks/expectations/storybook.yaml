# Expectations for storybookjs/storybook benchmark.
# Stress-tier: large monorepo, results may vary.
# Expectations should be tightened after initial calibration run.
min_test_files: 0
min_code_units: 0
require_posture: false
analyze_must_succeed: true
