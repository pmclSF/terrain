describe('weak tests', () => {
  it('should do something', () => {
    const result = processData();
    // No assertions - just execution
  });

  it('should handle edge case', () => {
    const output = handleEdge();
  });

  it('at least this one asserts', () => {
    expect(true).toBe(true);
  });
});
