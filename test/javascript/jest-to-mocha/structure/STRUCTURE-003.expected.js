const { expect } = require('chai');

describe('Database', () => {
  before(() => {
    // setup
  });

  after(() => {
    // teardown
  });

  it('connects', () => {
    expect(true).to.be.true;
  });
});
