describe('App', () => {
  beforeAll(() => {
    // init
  });

  beforeEach(() => {
    // per test
  });

  afterEach(() => {
    // per test cleanup
  });

  afterAll(() => {
    // final cleanup
  });

  it('works', () => {
    expect('hello').toBe('hello');
  });
});
