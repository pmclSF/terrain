const { expect } = require('chai');

describe('App', () => {
  before(() => {
    // init
  });

  beforeEach(() => {
    // per test
  });

  afterEach(() => {
    // per test cleanup
  });

  after(() => {
    // final cleanup
  });

  it('works', () => {
    expect('hello').to.equal('hello');
  });
});
