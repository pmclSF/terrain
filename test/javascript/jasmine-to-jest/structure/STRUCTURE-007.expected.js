describe('lifecycle', () => {
  beforeAll(() => {
    // setup once
  });
  beforeEach(() => {
    // setup
  });
  afterEach(() => {
    // teardown
  });
  afterAll(() => {
    // teardown once
  });
  it('works', () => {
    expect(true).toBe(true);
  });
});
