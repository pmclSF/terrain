describe('test', function() {
  // TERRAIN-WARNING: Mocha this.timeout(10000) — use jest.setTimeout(10000) at top of file or per-test timeout option
// Original: this.timeout(10000);
// this.timeout(10000);
  it('works', () => {
    expect(true).toBe(true);
  });
});
