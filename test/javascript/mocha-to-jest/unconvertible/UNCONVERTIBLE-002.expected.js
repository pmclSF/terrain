describe('test', () => {
  it('server', () => {
    // HAMLET-TODO [UNCONVERTIBLE-FAKE-SERVER]: sinon.fakeServer has no direct Jest equivalent
// Original: sinon.fakeServer.create();
// Manual action required: Use msw (Mock Service Worker) or jest-fetch-mock instead
// sinon.fakeServer.create();
  });
});
