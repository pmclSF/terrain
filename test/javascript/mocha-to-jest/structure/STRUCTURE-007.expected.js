describe('App', () => {
  beforeAll(() => {
    // init
  });

  describe('feature A', () => {
    it('works', () => {
      expect('a').toBe('a');
    });
  });

  afterAll(() => {
    // cleanup
  });
});
