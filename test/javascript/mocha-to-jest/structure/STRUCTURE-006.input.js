const { expect } = require('chai');

describe('Test', () => {
  beforeEach(() => {
    // setup
  });

  afterEach(() => {
    // cleanup
  });

  it('works', () => {
    expect(1).to.equal(1);
  });
});
