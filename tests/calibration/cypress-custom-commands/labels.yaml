schemaVersion: 1
fixture: cypress-custom-commands
description: |
  A Cypress spec installs a custom command via Cypress.Commands.add()
  before its describe block. Custom commands are Cypress-specific
  setup that other frameworks don't have a direct equivalent for, so
  UnsupportedSetupDetector flags it.
expected:
  - type: unsupportedSetup
    file: src/login.test.js
    notes: "Cypress.Commands.add — framework-specific custom command"
  - type: migrationBlocker
    file: src/login.test.js
    notes: "unsupported setup escalates to a hard migration blocker"
