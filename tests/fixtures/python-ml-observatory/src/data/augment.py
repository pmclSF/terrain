"""Data augmentation utilities."""

def synonym_replace(text, n=1):
    """Replace n words with synonyms (stub)."""
    return text

def random_insert(text, n=1):
    """Insert n random words (stub)."""
    return text + " augmented"

def back_translate(text, src="en", tgt="de"):
    """Back-translate text for augmentation (stub)."""
    return text
