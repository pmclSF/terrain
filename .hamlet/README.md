# .hamlet/

Local Hamlet state directory. Used for:

- `snapshots/` -- persisted TestSuiteSnapshot JSON files (e.g., `latest.json`)
- `policy.yaml` -- local policy configuration

This directory is repo-local and typically gitignored for user repos. In the Hamlet repo itself it is checked in as scaffolding.
