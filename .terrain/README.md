# .terrain/

Local Terrain state directory. Used for:

- `snapshots/` -- persisted TestSuiteSnapshot JSON files (e.g., `latest.json`)
- `policy.yaml` -- local policy configuration

This directory is repo-local and typically gitignored for user repos. In the Terrain repo itself it is checked in as scaffolding.
