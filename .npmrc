# Enforce exact versions when adding new production dependencies.
# This prevents caret/tilde ranges from introducing version drift.
# Dev dependencies are exempt (contributors manage via Dependabot PRs).
save-exact=true

# Require the lockfile to be present and up-to-date for installs.
# Prevents accidental `npm install` from modifying the lockfile.
package-lock=true
